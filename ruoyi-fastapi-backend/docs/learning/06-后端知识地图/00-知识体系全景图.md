# 知识体系全景图

## 完整知识体系

```mermaid
%%{init: {'theme': 'base', 'themeVariables': {
  'primaryColor': '#3B82F6',
  'primaryTextColor': '#fff',
  'primaryBorderColor': '#2563EB',
  'lineColor': '#6B7280',
  'secondaryColor': '#10B981',
  'tertiaryColor': '#F59E0B',
  'background': '#f8fafc',
  'fontSize': '14px'
}}}%%
mindmap
  root((后端知识地图))
    思维培养体系
      数据建模思维
        三层建模: 概念→逻辑→物理
        范式理论: 1NF/2NF/3NF
        关系设计: 1:1/1:N/M:N/自关联
        字段设计: 命名/类型/约束/审计
        索引设计: 类型选择/性能权衡
      接口设计思维
        核心原则: 简单/一致/可预测
        URI设计: 资源命名/层级结构
        HTTP方法: GET/POST/PUT/PATCH/DELETE
        参数设计: 路径/查询/请求体
        响应设计: 统一格式/数据脱敏
        错误处理: 错误码/错误信息
        版本管理: URL版本/兼容性
      性能优化思维
        应用层: 缓存/异步/批量
        数据库层: 索引/查询/事务
        网络层: 压缩/分页/请求合并
        代码层: 算法/字符串优化
        监控: 性能分析工具
      安全考虑思维
        安全原则: 不信任输入/最小权限/纵深防御
        输入验证: 白名单/Pydantic/SQL注入防护
        认证授权: bcrypt哈希/JWT/Token刷新
        权限控制: 接口权限/数据权限/越权防护
        数据保护: 加密/脱敏/HTTPS
        攻击防护: XSS/CSRF/暴力破解/限流
      可维护性思维
        代码组织: 分层架构/单一职责/模块化
        命名规范: 变量/函数/类
        注释文档: 文档字符串/行内注释
        错误处理: 明确信息/自定义异常
        测试: 单元测试/集成测试
        配置管理: 环境变量/配置类/多环境
        代码审查: 功能/可读性/可维护性/性能/安全
    前端开发者入门
      思维转变
        HTTP请求-响应: 无状态协议/请求响应流程
        路由映射: URL到函数/路径参数
        参数类型: 查询/路径/请求体参数
        统一响应: code/msg/rows/total
        Pydantic验证: 数据验证/序列化
        数据脱敏
      数据处理
        三种模型: DO/VO/Query
        ORM本质: 对象关系映射
        类型系统: Pydantic=TypeScript
        协作流程: Query→DO→VO
      接口设计
        RESTful: HTTP方法语义/资源命名规范
        路由分组: APIRouter/模块化设计
        Controller职责: 单一职责/参数处理/返回响应
        参数校验: Pydantic层/业务逻辑层/权限层
        设计模式: 分页/批量/树形结构
      依赖管理
        Depends机制: 函数参数自动管理
        价值: 资源管理/逻辑复用/代码解耦
        依赖链: 嵌套执行/Service层
        应用: 数据库会话/权限验证/日志记录
      异步编程
        async/await语法
        异步vs同步/并发vs并行
        异步上下文管理器: async with
        asyncio.gather: 并发执行
        数据库操作异步化/超时控制/错误处理
      数据持久化
        CRUD操作: 异步CRUD
        事务ACID: 原子/一致/隔离/持久
        ORM查询: 条件/关联/动态查询
        分页查询实现
        软删除vs硬删除
        Redis缓存策略
      实战项目
        数据库表: Transaction/Account/Category/Budget
        完整CRUD接口
        复杂业务: 账户余额联动
        统计查询: 聚合函数
        事务处理: 数据一致性
```

## 核心知识领域

| 领域 | 核心内容 |
|-----|---------|
| 数据建模 | 三层建模(概念→逻辑→物理)、关系设计(1:1/1:N/M:N)、DO/VO/Query三种模型 |
| 接口设计 | RESTful风格、统一响应(code/msg/rows/total)、版本管理 |
| 性能优化 | 应用层(缓存/异步/批量)、数据库层(索引/查询)、网络层(压缩/分页) |
| 安全防护 | 认证授权(JWT/OAuth2)、输入验证(Pydantic)、攻击防护(XSS/CSRF/限流) |
| 异步编程 | async/await、asyncio.gather、异步IO、非阻塞操作 |
| 数据持久化 | CRUD操作、事务管理(ACID)、缓存策略(Redis) |

## 学习路径

### 初学者
前端开发者入门 → 思维培养体系 → 专题深入

### 有经验开发者
思维培养体系 → 专题文档查漏补缺 → 实践项目应用
